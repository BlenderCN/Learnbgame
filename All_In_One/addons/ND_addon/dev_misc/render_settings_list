rd=scene.render
cy=scene.cycles

#render
cy.feature_set
cy.device
cy.shading_system


#dimensions
rd.resolution_x
rd.resolution_y

rd.image_settings.file_format
rd.image_settings.color_mode
rd.image_settings.color_depth
rd.image_settings.exr_codec
rd.image_settings.compression


#sampling
cy.progressive

cy.use_square_samples
cy.seed
cy.use_animated_seed
cy.sample_clamp_direct
cy.sample_clamp_indirect
cy.light_sampling_threshold
cy.aa_samples
cy.preview_aa_samples
cy.sample_all_lights_direct
cy.sample_all_lights_indirect
cy.sampling_pattern

cy.samples
cy.preview_samples

cy.diffuse_samples
cy.glossy_samples
cy.transmission_samples
cy.ao_samples
cy.mesh_light_samples
cy.subsurface_samples
cy.volume_samples

#geometry
cy.volume_step_size
cy.volume_max_steps

cy.curves.use_curves

cy.curves.primitive
cy.curves.shape
cy.curves.cull_backfacing
cy.curves.minimum_width
cy.curves.maximum_width
cy.curves.subdivisions

#ligth paths
cy.transparent_max_bounces
cy.transparent_min_bounces
cy.use_transparent_shadows
cy.caustics_reflective
cy.caustics_refractive
cy.blur_glossy
cy.max_bounces
cy.min_bounces
cy.diffuse_bounces
cy.glossy_bounces
cy.transmission_bounces
cy.volume_bounces

#motion blur
rd.use_motion_blur
cy.motion_blur_position
cy.rolling_shutter_type
cy.rolling_shutter_duration

#film
cy.film_exposure
cy.film_transparent
cy.pixel_filter_type
cy.filter_width

#performance
rd.threads_mode
rd.threads
cy.tile_order
rd.tile_x
rd.tile_y
cy.use_progressive_refine
rd.use_save_buffers
cy.debug_bvh_type
cy.preview_start_resolution
rd.use_persistent_data
cy.debug_use_spatial_splits
cy.debug_use_hair_bvh
cy.debug_bvh_time_steps

#post processing
rd.use_compositing
rd.use_sequencer
rd.dither_intensity