# 原则：
	#代码尽可能的接近自然语言
 	#代码尽可能地通俗易懂

# 个体
class body(object):
	"""docstring for body"""
	def __init__(self, arg):
		super(body, self).__init__()
		self.Hydrogen=False
"""		
		.
		.
		.
		判断是否含有某种元素，共有118种元素，如果有，则为True，表现为元素周期表相应位置出现动态三维模型，名称,原型图片高亮，否则为False,默认为False
		



图鉴：
	化学元素图鉴——元素周期表，
	物种图鉴——动物,植物...

因为游戏中的很多元素都具有客观性，不能随意更改，所以相关的修改引入区块链技术
"""

class Atom(object):
	"""docstring for Atom"""
	def __init__(self, arg):
		super(Atom, self).__init__()
		self.name = "" # 元素周期表的里的118种元素的名称
