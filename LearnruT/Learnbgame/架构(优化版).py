# 原则：
	#代码尽可能的少，
	#代码尽可能地高效，
	#代码尽可能地简洁，

# 个体
class body(object):
	"""docstring for body"""
	def __init__(self, arg):
		super(body, self).__init__()
		self.PeriodicTable = PeriodicTable
"""		
		.
		.
		.
		判断是否含有某种元素，共有118种元素，如果有，则为True，表现为元素周期表相应位置出现动态三维模型，名称高亮，否则为False,
		

"""

图鉴：
	化学元素图鉴——元素周期表，
	物种图鉴——动物,植物...

因为游戏中的很多元素都具有客观性，不能随意更改，所以相关的修改引入区块链技术

class PeriodicTable(object):
	"""docstring for PeriodicTable"""
	def __init__(self, arg):
		super(PeriodicTable, self).__init__()
		self.arg = arg
		
class Atom(object):
	"""docstring for Atom"""
	def __init__(self, arg):
		super(Atom, self).__init__()
		self.name = "" # 元素周期表的里的118种元素的名称
